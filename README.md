# Space Analytics Platform

Полнофункциональное приложение для отслеживания космических данных, включая местоположение МКС, астрономические события, снимки телескопа Джеймса Уэбба (JWST) и данные из открытого репозитория научных данных (OSDR).

## 🚀 Возможности

- **Отслеживание МКС**: Отслеживание местоположения Международной космической станции в реальном времени и телеметрические показатели с использованием карт Leaflet.
- **Астрономические события**: Календарь и подробная информация о предстоящих астрономических событиях.
- **Галерея JWST**: Просмотр последних изображений с космического телескопа Джеймса Уэбба.
- **Данные OSDR**: Доступ к наборам данных из Open Science Data Repository.
- **Космический дашборд**: Сводная панель ключевых космических показателей.
- **Интерактивные графики**: Визуализация данных с использованием Recharts.

## 🛠 Технологический стек

### Backend (Бэкенд)
- **Фреймворк**: FastAPI (Python 3.10+)
- **База данных**: PostgreSQL с SQLAlchemy (Async) и миграциями Alembic
- **Планировщик задач**: APScheduler для фонового сбора данных
- **Валидация**: Pydantic

### Frontend (Фронтенд)
- **Фреймворк**: Next.js 16 (React 19)
- **Стилизация**: Tailwind CSS, Shadcn UI (Radix UI)
- **Карты**: React Leaflet
- **Графики**: Recharts
- **Состояние/Запросы**: React Hooks

### DevOps
- **Контейнеризация**: Docker и Docker Compose

## 🏁 Начало работы

### Предварительные требования
- Docker и Docker Compose
- Или: Python 3.10+ и Node.js 18+ (для ручной установки)

### Запуск с Docker (Рекомендуется)

1. Клонируйте репозиторий:
   ```bash
   git clone <repository-url>
   cd frameworks3
   ```

2. Настройте переменные окружения:
   Скопируйте `.env.example` в `.env` (обычно обрабатывается автоматически через docker-compose, но полезно иметь):
   ```bash
   cp .env.example .env
   ```

3. Соберите и запустите контейнеры:
   ```bash
   docker-compose up --build
   ```

   Приложение будет доступно по адресам:
   - Frontend: http://localhost:3000
   - Backend API: http://localhost:8000
   - Документация API: http://localhost:8000/docs

### Ручная установка

#### Backend

1. Перейдите в директорию бэкенда:
   ```bash
   cd backend
   ```

2. Создайте виртуальное окружение и установите зависимости:
   ```bash
   python -m venv venv
   source venv/bin/activate  # В Windows: venv\Scripts\activate
   pip install -r requirements.txt
   ```

3. Запустите сервер:
   ```bash
   uvicorn app.main:app --reload
   ```

#### Frontend

1. Перейдите в директорию фронтенда:
   ```bash
   cd frontend
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

3. Запустите сервер разработки:
   ```bash
   npm run dev
   ```

## 📂 Структура проекта

```
├── backend/            # Приложение FastAPI (бэкенд)
│   ├── app/            # Исходный код приложения
│   │   ├── api/        # API эндпоинты
│   │   ├── core/       # Базовая конфигурация и утилиты
│   │   ├── models/     # Модели базы данных
│   │   └── services/   # Бизнес-логика
│   └── tests/          # Тесты Pytest
│
├── frontend/           # Приложение Next.js (фронтенд)
│   ├── app/            # Страницы App Router
│   ├── components/     # React компоненты
│   └── hooks/          # Пользовательские хуки
│
└── docker-compose.yml  # Конфигурация Docker Compose
```
